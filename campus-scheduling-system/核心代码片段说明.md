# 校园智能排班与通知系统 - 核心代码片段说明

## 1. 智能排班算法

**功能说明**：根据成员空闲时间、优先级、部门归属等因素自动生成排班，确保公平合理。

**核心代码**：

```javascript
// 生成排班
router.post('/', async (req, res) => {
  const { task_id } = req.body;

  try {
    // 获取任务信息
    const task = await new Promise((resolve, reject) => {
      db.get('SELECT * FROM tasks WHERE id = ?', [task_id], (err, task) => {
        if (err) reject(err);
        else resolve(task);
      });
    });

    // 获取任务时间信息
    const task_start_time = new Date(task.start_time);
    const task_end_time = new Date(task.end_time);
    const day_of_week = task_start_time.getDay();
    const start_time = task_start_time.toTimeString().substring(0, 5);
    const end_time = task_end_time.toTimeString().substring(0, 5);

    // 获取部门成员和所有成员
    const department_members = await new Promise((resolve, reject) => {
      db.all('SELECT * FROM members WHERE department_id = ?', [task.department_id], (err, members) => {
        if (err) reject(err);
        else resolve(members);
      });
    });

    // 过滤并排序成员
    const available_members = [];
    
    // 先处理本部门成员
    for (const member of department_members) {
      const has_conflict = await checkTimeConflict(member.id, task.start_time, task.end_time);
      const has_availability = await checkAvailability(member.id, day_of_week, start_time, end_time);
      const duty_count = await getMemberDutyCount(member.id, start_date, end_date);
      
      if (!has_conflict && has_availability) {
        available_members.push({ ...member, duty_count, is_department: true });
      }
    }

    // 排序：核心成员优先，然后按值班次数排序
    available_members.sort((a, b) => {
      // 核心成员优先
      if (a.priority === 'core' && b.priority !== 'core') return -1;
      if (a.priority !== 'core' && b.priority === 'core') return 1;
      // 本部门成员优先
      if (a.is_department && !b.is_department) return -1;
      if (!a.is_department && b.is_department) return 1;
      // 值班次数少的优先
      return a.duty_count - b.duty_count;
    });

    // 创建排班记录
    const schedules = [];
    for (let i = 0; i < Math.min(task.required_count, available_members.length); i++) {
      const member = available_members[i];
      const schedule = await new Promise((resolve, reject) => {
        db.run(
          'INSERT INTO schedules (task_id, member_id, status) VALUES (?, ?, ?)',
          [task_id, member.id, 'assigned'],
          function(err) {
            if (err) reject(err);
            else resolve({ id: this.lastID, task_id, member_id: member.id, status: 'assigned' });
          }
        );
      });
      schedules.push(schedule);
    }

    res.status(201).json(schedules);
  } catch (err) {
    res.status(500).json({ error: 'Database error' });
  }
});
```

**实现逻辑**：
1. 获取任务信息和时间细节
2. 首先从任务所属部门选择成员
3. 检查成员是否有时间冲突和空闲时间
4. 计算成员近期值班次数，确保公平性
5. 如果本部门成员不足，从其他部门调配
6. 按照优先级、部门归属、值班次数排序
7. 创建排班记录并返回结果

## 2. 时间冲突校验

**功能说明**：检查成员是否在指定时间段已有其他排班，避免时间冲突。

**核心代码**：

```javascript
// 检查成员是否有时间冲突
const checkTimeConflict = (member_id, task_start_time, task_end_time, exclude_schedule_id = null) => {
  return new Promise((resolve, reject) => {
    const query = exclude_schedule_id 
      ? 'SELECT COUNT(*) as count FROM schedules s JOIN tasks t ON s.task_id = t.id WHERE s.member_id = ? AND s.id != ? AND ((t.start_time <= ? AND t.end_time >= ?) OR (t.start_time <= ? AND t.end_time >= ?))'
      : 'SELECT COUNT(*) as count FROM schedules s JOIN tasks t ON s.task_id = t.id WHERE s.member_id = ? AND ((t.start_time <= ? AND t.end_time >= ?) OR (t.start_time <= ? AND t.end_time >= ?))';
    const params = exclude_schedule_id 
      ? [member_id, exclude_schedule_id, task_end_time, task_start_time, task_end_time, task_start_time]
      : [member_id, task_end_time, task_start_time, task_end_time, task_start_time];
    
    db.get(query, params, (err, result) => {
      if (err) {
        reject(err);
      } else {
        resolve(result.count > 0);
      }
    });
  });
};

// 检查成员是否有空闲时间
const checkAvailability = (member_id, day_of_week, start_time, end_time) => {
  return new Promise((resolve, reject) => {
    db.get(
      'SELECT COUNT(*) as count FROM availability WHERE member_id = ? AND day_of_week = ? AND start_time <= ? AND end_time >= ?',
      [member_id, day_of_week, end_time, start_time],
      (err, result) => {
        if (err) {
          reject(err);
        } else {
          resolve(result.count > 0);
        }
      }
    );
  });
};
```

**实现逻辑**：
1. `checkTimeConflict` 函数：查询成员在指定时间段是否已有其他排班
2. `checkAvailability` 函数：检查成员是否在该时间段设置了空闲时间
3. 两个函数都返回 Promise，方便在异步流程中使用
4. 使用 SQL 查询检查时间重叠情况，确保排班合理

## 3. 通知发送功能

**功能说明**：支持多种渠道发送通知，确保成员及时收到排班信息，并处理发送失败的情况。

**核心代码**：

```javascript
// 发送通知并更新状态
const sendNotification = async (notification) => {
  const { id, channel, content, member_id } = notification;
  
  try {
    if (channel === 'email') {
      await sendEmail('user@example.com', '排班通知', content);
    } else if (channel === 'wechat') {
      await sendWechatNotification(member_id, content);
    } else if (channel === 'qywechat') {
      await sendQyWechatNotification(member_id, content);
    }
    
    db.run(
      'UPDATE notifications SET status = ?, sent_at = ? WHERE id = ?',
      ['sent', new Date().toISOString(), id]
    );
    
    return { success: true };
  } catch (error) {
    db.run(
      'UPDATE notifications SET status = ? WHERE id = ?',
      ['failed', id]
    );
    return { success: false, error };
  }
};

// 重新发送通知
router.post('/:id/resend', (req, res) => {
  const { id } = req.params;
  
  db.get('SELECT * FROM notifications WHERE id = ?', [id], (err, notification) => {
    if (err) {
      return res.status(500).json({ error: 'Database error' });
    }
    
    if (!notification) {
      return res.status(404).json({ error: 'Notification not found' });
    }
    
    db.run(
      'UPDATE notifications SET status = ?, sent_at = NULL WHERE id = ?',
      ['pending', id],
      (updateErr) => {
        if (updateErr) {
          return res.status(500).json({ error: 'Failed to update notification' });
        }
        
        const updatedNotification = { ...notification, status: 'pending', sent_at: null };
        sendNotification(updatedNotification);
        
        res.json({ message: 'Notification resent successfully' });
      }
    );
  });
});
```

**实现逻辑**：
1. `sendNotification` 函数：根据通知渠道发送通知
2. 支持邮件、微信、企业微信三种通知方式
3. 发送成功后更新通知状态为已发送
4. 发送失败时更新通知状态为失败
5. `resend` 接口：重新发送失败的通知

## 4. Excel 数据导出

**功能说明**：导出完整的统计数据到 Excel 文件，包含部门、成员和志愿时长统计。

**核心代码**：

```javascript
// 导出Excel表格
router.get('/export', async (req, res) => {
  try {
    const workbook = new ExcelJS.Workbook();
    const departmentsSheet = workbook.addWorksheet('部门统计');
    const membersSheet = workbook.addWorksheet('成员统计');
    const volunteerSheet = workbook.addWorksheet('志愿时长统计');

    departmentsSheet.columns = [
      { header: '部门ID', key: 'id', width: 12 },
      { header: '部门名称', key: 'name', width: 25 },
      { header: '任务数量', key: 'task_count', width: 15 },
      { header: '排班数量', key: 'schedule_count', width: 15 }
    ];

    membersSheet.columns = [
      { header: '成员ID', key: 'id', width: 12 },
      { header: '成员姓名', key: 'name', width: 20 },
      { header: '部门ID', key: 'department_id', width: 12 },
      { header: '排班次数', key: 'schedule_count', width: 15 },
      { header: '出勤次数', key: 'attendance_count', width: 15 },
      { header: '出勤率(%)', key: 'attendance_rate', width: 15 }
    ];

    volunteerSheet.columns = [
      { header: '成员ID', key: 'id', width: 12 },
      { header: '成员姓名', key: 'name', width: 20 },
      { header: '部门ID', key: 'department_id', width: 12 },
      { header: '总志愿时长(小时)', key: 'total_hours', width: 20 }
    ];

    // 获取部门统计数据
    const departments = await new Promise((resolve, reject) => {
      db.all(
        `
        SELECT d.id, d.name, COUNT(t.id) as task_count, COUNT(s.id) as schedule_count
        FROM departments d
        LEFT JOIN tasks t ON d.id = t.department_id
        LEFT JOIN schedules s ON t.id = s.task_id
        GROUP BY d.id, d.name
        `,
        (err, rows) => {
          if (err) reject(err);
          else resolve(rows);
        }
      );
    });

    // 添加数据到表格
    departments.forEach(dept => {
      departmentsSheet.addRow(dept);
    });

    // 设置表头样式
    applyHeaderStyle(departmentsSheet);
    applyHeaderStyle(membersSheet);
    applyHeaderStyle(volunteerSheet);

    // 发送Excel文件
    res.setHeader('Content-Type', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');
    res.setHeader('Content-Disposition', 'attachment; filename=statistics.xlsx');
    
    await workbook.xlsx.write(res);
    res.end();
  } catch (error) {
    console.error('Export error:', error);
    res.status(500).json({ error: 'Failed to export Excel file' });
  }
});
```

**实现逻辑**：
1. 创建 Excel 工作簿和多个工作表
2. 设置各工作表的列头和格式
3. 从数据库获取统计数据
4. 将数据添加到对应工作表
5. 设置表头样式，美化表格
6. 将 Excel 文件发送给客户端

## 5. 请假申请处理

**功能说明**：处理成员的请假申请，包括审批和自动重新排班。

**核心代码**：

```javascript
// 处理请假申请
router.post('/', (req, res) => {
  const { schedule_id, member_id, reason, start_time, end_time } = req.body;

  db.run(
    'INSERT INTO leave_requests (schedule_id, member_id, reason, start_time, end_time, status) VALUES (?, ?, ?, ?, ?, ?)',
    [schedule_id, member_id, reason, start_time, end_time, 'pending'],
    function(err) {
      if (err) {
        return res.status(500).json({ error: 'Database error' });
      }
      
      // 这里可以添加自动重新排班的逻辑
      res.status(201).json({ id: this.lastID, schedule_id, member_id, status: 'pending' });
    }
  );
});

// 审批请假申请
router.put('/:id/approve', async (req, res) => {
  const { id } = req.params;
  const { approved_by } = req.body;

  try {
    // 获取请假申请信息
    const leaveRequest = await new Promise((resolve, reject) => {
      db.get('SELECT * FROM leave_requests WHERE id = ?', [id], (err, leaveRequest) => {
        if (err) reject(err);
        else resolve(leaveRequest);
      });
    });

    if (!leaveRequest) {
      return res.status(404).json({ error: 'Leave request not found' });
    }

    // 更新请假申请状态
    await new Promise((resolve, reject) => {
      db.run(
        'UPDATE leave_requests SET status = ?, approved_by = ? WHERE id = ?',
        ['approved', approved_by, id],
        function(err) {
          if (err) reject(err);
          else resolve();
        }
      );
    });

    // 这里可以添加自动重新排班的逻辑
    // ...

    res.json({ id, status: 'approved' });
  } catch (err) {
    res.status(500).json({ error: 'Database error' });
  }
});
```

**实现逻辑**：
1. 接收成员的请假申请，记录到数据库
2. 审批请假申请，更新状态
3. 可以集成自动重新排班功能，确保值班岗位不空缺
4. 保持请假记录，方便后续统计

## 技术总结

本系统采用了现代化的技术栈和架构设计：

- **前端**：React.js + Tailwind CSS，实现了美观的用户界面
- **后端**：Express.js，提供了 RESTful API 接口
- **数据库**：SQLite，轻量级且易于部署
- **核心功能**：智能排班、冲突检测、多渠道通知、数据统计、Excel导出

系统设计考虑了大学生的使用场景，操作简单直观，功能完善，能够满足校园组织的排班和管理需求。